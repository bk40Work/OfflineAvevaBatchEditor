{
  "name": "aveva-recipe-editor-tests",
  "private": true,
  "description": "Automated tests for the offline AVEVA Batch recipe editor core (runs the browser modules in Node).",
  "scripts": {
    "test": "node test-roundtrip.js && node test-scenarios.js && node test-edits.js",
    "test:ui": "python3 ui/test_ui.py"
  },
  "devDependencies": {
    "@xmldom/xmldom": "^0.8.10"
  }
}
