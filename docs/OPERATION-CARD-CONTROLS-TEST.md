# Operation card controls — test

1. Enter Edit mode and select a Unit Procedure.
2. Confirm each Operation card has `?` and `⋮`, with no duplicated arrow before End.
3. Select `?`: confirm Operation Name is editable and the AVEVA RecipeElement ID is displayed as the read-only node identity.
4. Change an Operation Name, export and reload; confirm the name persists.
5. Open `⋮`: it provides the Operation action surface. Creation/deletion entries are intentionally disabled until their XML-authoritative adapters are delivered.
6. Repeat the Operation drag test. Phase movement must be unchanged.
